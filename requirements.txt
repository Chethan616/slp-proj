# Core inference dependencies, shared by every front end.
# Deliberately no torch: the classifier runs as an int8 ONNX graph and Whisper
# runs on CTranslate2, so the ~2.5 GB CUDA-enabled torch wheel is never needed.
faster-whisper>=1.1.0
onnxruntime>=1.19
transformers>=4.48
huggingface-hub>=0.26
numpy>=2.0
